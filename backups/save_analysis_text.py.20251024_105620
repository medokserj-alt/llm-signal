#!/usr/bin/env python3
import sys, re, os, json
from datetime import datetime, timezone
from pathlib import Path

def _canon_block_from_json(json_path="logs/last.json"):
    try:
        d = json.loads(Path(json_path).read_text(encoding="utf-8"))
    except Exception:
        return ""

    lines = []
    em = d.get("entry_mode")
    cf = d.get("confidence")
    if em: lines.append(f"💡 Режим входа: {em}")
    if cf: lines.append(f"🎯 Уверенность: {cf}")

    conf = d.get("confirmation_rules")
    if conf:
        conf = " ".join(str(conf).split())
        lines.append(f"☑ Подтверждение: {conf}")

    # ВАЖНО: альтернативный вход в текст не добавляем
    return ("\n".join(lines) + "\n") if lines else ""

def _dedup_setup_blocks(text):
    # Схлопываем повторы блока без "🔁"
    pattern = r"(?:^|\n)(💡 Режим входа:[^\n]*\n🎯 Уверенность:[^\n]*(?:\n☑ Подтверждение:[^\n]*)?\n)"
    while True:
        new = re.sub(pattern + r"\s*" + pattern, r"\n\1", text, flags=re.M)
        if new == text:
            break
        text = new
    return text

def main():
    data = sys.stdin.read()

    # 1) Обрезаем всё после первого JSON-блока
    cut_idx = data.find("\\n{")
    if cut_idx != -1:
        analysis = data[:cut_idx]
    else:
        m = re.search(r'(?m)^\\s*\\{', data)
        analysis = data[:m.start()] if m else data

    # 2) Чистим code fences
    analysis = re.sub(r'```+\\w*\\s*\\n?', '', analysis)

    # 3) Сжимаем пустые строки
    analysis = re.sub(r'\\n{3,}', '\\n\\n', analysis).strip() + "\\n"

    # 4) Канонический блок из JSON
    canon = _canon_block_from_json()
    if canon:
        m = re.search(r'(?m)^2️⃣[^\\n]*Сетап[^\\n]*\\n', analysis)
        if m:
            start = m.end()
            # вырежем старые маркеры 💡/🎯/☑/🔁 и вставим канон
            after = analysis[start:]
            after = re.sub(r'(?m)^(💡|🎯|☑|🔁)[^\\n]*\\n', '', after)
            analysis = analysis[:start] + canon + after
        else:
            analysis = canon + "\\n" + analysis
        analysis = _dedup_setup_blocks(analysis)

    # 5) Сохраняем
    ts = datetime.now(timezone.utc).strftime("%Y%m%d_%H%M%S")
    fname = f"analysis_{ts}.md"
    Path(fname).write_text(analysis, encoding="utf-8")
    print(f"[OK] ANALYSIS: {fname}")

if __name__ == "__main__":
    main()
